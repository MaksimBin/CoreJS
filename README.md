
Core Framework

Core — это минималистичный UI‑движок, вдохновлённый React, но работающий без сборки и Babel. Он создан для экспериментов с виртуальным DOM, хуками и декларативным синтаксисом.

✨ Возможности

Виртуальный DOM и diff‑алгоритм

Компоненты и реестр компонентов

Хук useState

Шаблонные строки tpl для декларативного описания интерфейсов

Поддержка фрагментов, списков, условного рендера и спред‑пропсов

События (onClick, onInput и др.)

📂 Структура проекта

Core/
├── index.html
├── core.js
└── app.js

🚀 Быстрый старт

index.html

<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Core Test</title>
</head>
<body>
  <div id="app"></div>
  <script type="module" src="./app.js"></script>
</body>
</html>

app.js

import { tpl, render, useState, registerComponent } from "./core.js";

function Counter() {
  const [count, setCount] = useState(0);
  return tpl`
    <div>
      <span>${count}</span>
      <button onClick=${() => setCount(c => c + 1)}>+1</button>
    </div>
  `;
}

function App() {
  return tpl`
    <div>
      <h1>Пример</h1>
      ${Counter()}
    </div>
  `;
}

registerComponent("Counter", Counter);
registerComponent("App", App);

render(App, "#app");

🛠 Планы развития

Добавить useEffect

Контекст и глобальное состояние

Роутинг

Интеграция с Babel для полноценного JSX

Core — это твой собственный фреймворк, созданный для экспериментов.
